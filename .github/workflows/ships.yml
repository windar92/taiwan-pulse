name: ships-collect
on:
  schedule:
    - cron: "*/10 * * * *"
  workflow_dispatch:
jobs:
  collect:
    runs-on: ubuntu-latest
    steps:
      - name: trigger AIS collector
        run: |
          curl -sS -X POST "https://taiwan-pulse-five.vercel.app/api/ships-collect" --max-time 75 || true
